CREATE OR REPLACE VIEW vista_actividad_proyecto AS SELECT actividad_proyecto_id AS vista_actividad_proyecto_id,ap_nombre,fase,fases_id FROM (`fases` JOIN `actividad_proyecto`) WHERE (fases_id = ap_fase)